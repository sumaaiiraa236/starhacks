import os, json
print('Seed Pinecone placeholder: will use PINECONE_API_KEY if set in backend/.env')
# For the hackathon, the script will either push sample_facts.json to Pinecone or skip if key missing.
